import { Input } from "@/components/Input";

export default function Home() {
  return (
    <div>
      <Input
        label="Password"
        placeholder="Enter your password"
        type="password"
      ></Input>
    </div>
  );
}
